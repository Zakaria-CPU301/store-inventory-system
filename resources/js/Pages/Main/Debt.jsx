import Table from "@/Components/Partials/Table";
import App from "@/Layouts/App";

const Debt = () => {
    const columns = [{ key: "" }];
    return (
        <App>
        </App>
    );
};

export default Debt;
